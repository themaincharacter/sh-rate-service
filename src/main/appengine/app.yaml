runtime: java
env: standard

runtime_config:  # Optional
  jdk: openjdk8


beta_settings:
  enable_app_engine_apis: true

handlers:
- url: /.*
  script: this field is required, but ignored

manual_scaling:
  instances: 1